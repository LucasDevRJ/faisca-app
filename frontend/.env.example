# Copie para frontend/.env e preencha. Variáveis VITE_* vão para o bundle: nada secreto aqui.

VITE_API_URL=http://localhost:3333
# Mesma chave pública VAPID do backend
VITE_VAPID_PUBLIC_KEY=CHANGE_ME